"""Credit risk strategy build: bureau + internal attributes to a decisioning engine.

Modules
-------
config   Every constant that governs the model: time framework, bad definition,
         exclusions, binning thresholds, score scaling, validation acceptance.
schemas  Bronze schema inference, override rules and persistence.

See plans.md for the full build plan.
"""

__version__ = "0.1.0"
